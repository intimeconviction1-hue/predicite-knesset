// Module jouet, sans autre role que de servir de cible a la sonde de
// tests/helpers/option-mock.mjs. Si sa valeur revient simulee, c'est que la
// forme d'option essayee est celle que comprend le Node installe.
export const marqueur = 'reel';
